  
```mermaid  
flowchart LR  
  
    subgraph GitLab["GitLab (CI)"]      repo[changshin-api · dev branch]      iacRepo[changshin-iac · main branch]    end  
    subgraph CI["GitLab Pipeline (changshin-api)"]      ciApiBuild[job-nestjs · service_prefix=api<br/>build apps/ax-api → image]      ciBatchBuild[job-nestjs · service_prefix=batch<br/>build apps/batch → image]      ciApiFlux["job-flux · service_prefix=api<br/>flux push artifact ./k8s<br/>(OCI: 매니페스트 전체)"]  
    end  
    subgraph ECR["AWS ECR (account 687582709363)"]      ecrApi[changshin-api<br/>ax-api 이미지]  
      ecrApiDeploy[changshin-api-deploy<br/>Flux OCI artifact]      ecrBatch[changshin-batch<br/>batch 이미지]  
      ecrIac[changshin-iac<br/>인프라 Flux OCI]    end  
    subgraph EKS["EKS · changshin-dev"]      subgraph FluxSystem["ns: flux-system"]        ociApi[OCIRepository: api]        ksApi["Kustomization: api<br/>path: ./clusters/dev<br/>wait: true"]      end  
      subgraph DevNS["ns: dev   ⚠ D-017 의도는 'changshin'"]        depAx["Deployment: ax-api<br/>image: changshin-api:dev-{sha}"]        svcAx["Service: ax-api"]        ingAx["Ingress: ax-api<br/>changshin-api.dev.weplanet.co.kr"]        depBatch["Deployment: batch<br/>image: changshin-batch:development"]        svcBatch["Service: batch"]        esP["ExternalSecret: parameter-store<br/>(ax-api 가 정의)"]  
        esS["ExternalSecret: secrets-manager<br/>(ax-api 가 정의)"]  
        secP[(Secret: parameter-store)]        secS[(Secret: secrets-manager)]      end    end  
    subgraph AWS["AWS 리소스"]  
      alb[ALB k8s-default-1acb9e2c57]      r53[Route53 weplanet.co.kr]      rds[(RDS PostgreSQL<br/>changshin)]      valkey[(ElastiCache Valkey)]      ssm[SSM Parameter Store<br/>/changshin/dev/rsa/...<br/>/changshin/brightdata/...<br/>/changshin/openai/...]      sm[Secrets Manager<br/>changshin/dev/rds-changshin]    end  
    repo -->|push dev| ciApiBuild    repo -->|push dev| ciBatchBuild    repo -->|push dev| ciApiFlux  
    ciApiBuild -->|push image :dev-{sha}, :development| ecrApi    ciBatchBuild -->|push image :dev-{sha}, :development| ecrBatch    ciApiFlux -->|push OCI :development| ecrApiDeploy  
    iacRepo -.terraform apply.-> ECR    iacRepo -.terraform apply.-> EKS  
    ecrApiDeploy -->|매분 polling| ociApi    ociApi -->|stored artifact| ksApi    ksApi -->|build root kustomization<br/>= ax-api + batch + ExternalSecrets| DevNS  
    ecrApi -->|kubelet pull| depAx    ecrBatch -->|kubelet pull| depBatch  
    esP -->|fetch| ssm    esS -->|fetch| sm    esP -.creates.-> secP    esS -.creates.-> secS    depAx -->|secretKeyRef| secP    depAx -->|secretKeyRef| secS    depBatch -->|secretKeyRef| secP    depBatch -->|secretKeyRef| secS  
    r53 --> alb --> ingAx --> svcAx --> depAx    depAx -.연결.-> rds  
    depAx -.연결.-> valkey  
    depBatch -.연결.-> rds  
    depBatch -.연결.-> valkey  
```